# Нормализация переводов строк: в репозитории всегда LF.
# Без этого при работе с Windows + macOS git будет показывать
# "изменился весь файл" при каждом переключении машины.
* text=auto eol=lf

# Бинарные — не трогать
*.png binary
*.jpg binary
*.pdf binary
*.xlsx binary
*.parquet binary
