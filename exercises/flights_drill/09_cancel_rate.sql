/* DRILL 9 — доля через FILTER
   Посчитай долю ОТМЕНЁННЫХ рейсов (%) от общего числа рейсов. Одна строка.
   Столбцы: всего рейсов, отменённых, доля отменённых в % (round 2).
   Схема: flights (status). Отменён = status = 'Cancelled'.
   Подсказка: COUNT(*) FILTER (WHERE status='Cancelled') и не забудь 100.0. */

-- решение:

