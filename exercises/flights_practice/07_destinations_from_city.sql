/* =====================================================================
   PRACTICE 7. Куда летают из города
   Навык: JOIN + DISTINCT
   ---------------------------------------------------------------------
   Выведи список уникальных городов, КУДА можно улететь из Москвы
   (город вылета = 'Moscow').
   Столбец: город прилёта. Отсортируй по алфавиту.

   Лайфхак по схеме: используй view timetable (там уже есть departure_airport
   и arrival_airport), а города возьми из airports:
     timetable.departure_airport → airports (город вылета, фильтр 'Moscow')
     timetable.arrival_airport   → airports (город прилёта, его и выводим)

   Понадобится DISTINCT — рейсов много, а городов-назначений мало.
   (airports придётся приджойнить ДВАЖДЫ — для вылета и для прилёта,
    разными алиасами, как в самом первом задании.)
   ===================================================================== */

SELECT * FROM tim


