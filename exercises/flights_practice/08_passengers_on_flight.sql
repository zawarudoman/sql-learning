/* =====================================================================
   PRACTICE 8. Пассажиры конкретного рейса
   Навык: JOIN по цепочке
   ---------------------------------------------------------------------
   Выведи список пассажиров одного рейса (возьми любой flight_id, например
   из SELECT flight_id FROM flights LIMIT 5).
   Столбцы: имя пассажира, класс обслуживания, цена сегмента.
   Отсортируй по цене, дороже — сверху.

   Цепочка связей:
     segments (flight_id, ticket_no, fare_conditions, price)
       → tickets (ticket_no, passenger_name)

   То есть: берём сегменты нужного рейса и джойним билеты ради имени.

   Схема: segments (flight_id, ticket_no, fare_conditions, price),
          tickets (ticket_no, passenger_name).
   ===================================================================== */

-- твоё решение:

