/* =====================================================================
   ANALYST 5. Заполняемость рейсов (load factor)
   ---------------------------------------------------------------------
   БИЗНЕС-ВОПРОС:
   Насколько заполнены самолёты? Load factor = проданные места / всего мест.
   Это ключевая метрика авиакомпании.

   ЧТО ВЫВЕСТИ (для топ-20 самых загруженных рейсов):
   • flight_id,
   • сколько мест продано (посадочных талонов),
   • сколько всего мест в самолёте,
   • load factor в % (round, 1).
   Отсортируй по load factor, самые полные сверху.

   ГДЕ ДАННЫЕ:
   • продано мест на рейс = COUNT(*) из boarding_passes по flight_id;
   • всего мест в самолёте = COUNT(*) из seats по airplane_code;
   • какой самолёт на рейсе — снова через timetable (у него есть airplane_code!):
       timetable.flight_id ↔ timetable.airplane_code

   ТЕХНИКА (две независимые агрегации, потом соединяем):
   1) CTE sold:    flight_id → число посадочных;
   2) CTE cap:     airplane_code → число мест;
   3) главный:     timetable как "мостик" flight_id → airplane_code,
                   JOIN к sold и к cap, считаем %.
   Дробное деление не забудь (100.0). Ловушек нет — просто 3 кусочка.
   ===================================================================== */

-- твоё решение:

