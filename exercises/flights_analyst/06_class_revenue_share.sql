/* =====================================================================
   ANALYST 6. Доля классов обслуживания в выручке
   ---------------------------------------------------------------------
   БИЗНЕС-ВОПРОС:
   Сколько выручки приносит каждый класс (Economy/Comfort/Business)
   и какая его ДОЛЯ в общей выручке?

   ЧТО ВЫВЕСТИ:
   • класс (fare_conditions),
   • выручка класса (SUM price),
   • число сегментов,
   • доля класса в общей выручке, % (round, 1).
   Отсортируй по выручке, крупнейший сверху.

   ГДЕ ДАННЫЕ:
   • одна таблица segments (fare_conditions, price) — JOIN не нужен.

   ТЕХНИКА (важный приём — доля от общего):
   Чтобы получить "долю от ВСЕЙ выручки" рядом с каждым классом, нужен
   общий итог. Его даёт оконный агрегат БЕЗ рамки:
       SUM(SUM(price)) OVER ()   -- да, SUM от SUM: внутренний по группе,
                                 --  внешний оконный — по всем группам
   Звучит странно, но это стандартный приём: агрегат внутри GROUP BY,
   а оконная функция поверх него считает по всем группам.
   Не забудь 100.0 для дробного процента.
   ===================================================================== */

-- твоё решение:

