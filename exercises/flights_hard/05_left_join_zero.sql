/* =====================================================================
   HARD 5. Загрузка самолётов — ловушка исчезающих строк
   Темы: LEFT JOIN vs INNER JOIN, строки с нулём
   ---------------------------------------------------------------------
   ЗАДАЧА:
   Для КАЖДОЙ модели самолёта покажи, сколько мест класса Business.
   ВАЖНО: модели, где бизнес-класса НЕТ вообще, должны попасть в результат
   со значением 0 (а не пропасть!).
   Столбцы: модель, число бизнес-мест. Сортировка по модели.

   ⚠️ Главная ловушка задания:
   Самый очевидный способ (JOIN seats + WHERE fare_conditions='Business')
   ВЫБРОСИТ модели без бизнес-класса — они просто исчезнут из отчёта.
   А задача требует показать их с нулём.

   Подумай:
   • почему обычный (INNER) JOIN + WHERE теряет такие модели?
   • почему даже LEFT JOIN не спасёт, если условие 'Business' стоит в WHERE?
     (куда должно переехать условие про класс, чтобы строки не отсеялись?)

   Схема: airplanes (airplane_code, model), seats (airplane_code, fare_conditions).
   Проверь себя: в результате должно быть СТОЛЬКО ЖЕ моделей, сколько всего
   в airplanes (ни одна не должна потеряться).
   ===================================================================== */

-- твоё решение:

-- контроль: столько всего моделей должно остаться в ответе
-- SELECT COUNT(*) FROM bookings.airplanes;

