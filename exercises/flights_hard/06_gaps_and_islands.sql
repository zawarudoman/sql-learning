/* =====================================================================
   HARD 6. Самая долгая серия ежедневных рейсов     ⭐ ЗАДАЧА С ЗВЁЗДОЧКОЙ
   Темы: lag, признак разрыва, нарастающая сумма — «gaps and islands»
   ---------------------------------------------------------------------
   Это классическая аналитическая задача, её любят на собеседованиях.
   Не спеши — она собирается из приёмов, которые ты уже знаешь.

   ЗАДАЧА:
   Для маршрута 'PG0001' найди самую длинную серию ПОДРЯД ИДУЩИХ ДНЕЙ,
   когда рейс выполнялся каждый день без пропуска.
   Выведи: дату начала серии, дату конца, длину серии (в днях).

   Идея решения (подумай, как собрать):
   1. возьми уникальные ДНИ вылета этого маршрута (scheduled_departure::date);
   2. окном lag() посмотри предыдущий день;
   3. если текущий день = предыдущий + 1 → серия продолжается, иначе — разрыв;
   4. трюк: присвой каждой строке "номер группы" как нарастающую сумму
      признака разрыва (SUM(признак) OVER (ORDER BY день)) — у строк одной
      серии номер группы будет одинаковым;
   5. сгруппируй по этому номеру: MIN(день) = начало, MAX(день) = конец,
      COUNT(*) = длина;
   6. возьми самую длинную.

   Не факт, что получится с первого раза — это нормально. Хочешь разбор
   по шагам — напиши, распишу подробно. Но сначала попробуй сам, у тебя
   уже есть все кирпичи: lag, разность дат, SUM() OVER, GROUP BY.
   ===================================================================== */

-- твоё решение:

