/* =====================================================================
   Задание 11. Города только на вылет            ⭐ НОВАЯ ТЕМА: множества
   Тема: EXCEPT / UNION / INTERSECT
   ---------------------------------------------------------------------
   ОПЕРАЦИИ НАД МНОЖЕСТВАМИ (склеивают РЕЗУЛЬТАТЫ двух запросов):
     UNION      — объединение (дубли убирает)
     UNION ALL  — объединение (дубли оставляет — быстрее!)
     INTERSECT  — пересечение (что есть и там, и там)
     EXCEPT     — разность (что есть в первом, но НЕТ во втором)

   Требование: у обоих запросов одинаковое число столбцов и совместимые типы.

   ---------------------------------------------------------------------
   ЗАДАЧА:
   Найди города, ИЗ которых рейсы вылетают, но В которые не прилетают.
   Столбец: город.

   Подумай: это "множество городов вылета" МИНУС "множество городов прилёта".
   Прямо просится EXCEPT.

   Бонус (по желанию): переделай через NOT EXISTS и сравни планы (EXPLAIN).
   ===================================================================== */

-- твоё решение:

