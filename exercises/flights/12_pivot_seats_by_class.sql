/* =====================================================================
   Задание 12. Компоновка салона по классам      ⭐ НОВАЯ ТЕМА: пивот
   Тема: превратить строки в столбцы (MAX+CASE / COUNT FILTER)
   ---------------------------------------------------------------------
   ЗАДАЧА:
   Для каждой модели самолёта покажи количество мест КАЖДОГО класса
   в отдельных столбцах.
   Столбцы: модель, Economy, Comfort, Business, всего мест.

   То есть из "много строк по одной на место" надо получить одну строку
   на модель, где классы разложены по столбцам.

   Два способа — попробуй оба:
   1) классический:  COUNT(CASE WHEN fare_conditions = 'Economy' THEN 1 END)
   2) постгресовый:  COUNT(*) FILTER (WHERE fare_conditions = 'Economy')
      — это удобный синтаксис PostgreSQL, читается лучше.

   Схема: seats (airplane_code, seat_no, fare_conditions), airplanes (model).
   Классы: 'Economy', 'Comfort', 'Business'.
   ===================================================================== */

-- твоё решение:

