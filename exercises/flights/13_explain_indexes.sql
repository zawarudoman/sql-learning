/* =====================================================================
   Задание 13. Планы запросов и индексы        ⭐ НОВАЯ ТЕМА: оптимизация
   Тема: EXPLAIN (ANALYZE), Seq Scan vs Index Scan
   ---------------------------------------------------------------------
   Это задание — не "напиши запрос", а "исследуй". Самое важное в Фазе 1.

   ---------------------------------------------------------------------
   ШАГ 1. Посмотри план обычного запроса:

EXPLAIN ANALYZE
SELECT * FROM bookings.flights
WHERE status = 'Cancelled';

   Найди в выводе:
   • Seq Scan  — полный проход по таблице (читает ВСЁ);
   • Index Scan — поиск по индексу (читает только нужное);
   • строку "Execution Time: ... ms" — реальное время.
   Что там у тебя?

   ---------------------------------------------------------------------
   ШАГ 2. Сравни с поиском по первичному ключу:

EXPLAIN ANALYZE
SELECT * FROM bookings.flights WHERE flight_id = 12345;

   Здесь должен быть Index Scan (по PK индекс есть). Сравни Execution Time
   с шагом 1 — почувствуй разницу.

   ---------------------------------------------------------------------
   ШАГ 3. ⚠️ Ловушка "функция на колонке убивает индекс".
   Сравни два запроса за один день:

EXPLAIN ANALYZE
SELECT count(*) FROM bookings.flights
WHERE scheduled_departure::date = DATE '2025-08-15';        -- функция на колонке

EXPLAIN ANALYZE
SELECT count(*) FROM bookings.flights
WHERE scheduled_departure >= DATE '2025-08-15'
  AND scheduled_departure <  DATE '2025-08-16';             -- полуоткрытый интервал

   Результат одинаковый, а планы — разные. Почему? Запиши вывод сюда:




   ---------------------------------------------------------------------
   ШАГ 4. Создай индекс и посмотри, что изменилось:

CREATE INDEX idx_flights_status ON bookings.flights (status);
   -- потом снова EXPLAIN ANALYZE из ШАГА 1 — план поменялся? время?
   -- (индекс потом можно удалить: DROP INDEX bookings.idx_flights_status;)

   -- мой вывод:

   ---------------------------------------------------------------------
   ЧТО ЗАПОМНИТЬ:
   • Seq Scan не всегда зло — на маленькой таблице или когда нужны почти
     все строки, он быстрее индекса.
   • Индекс помогает, когда выбираешь МАЛУЮ долю строк.
   • Функция/каст на колонке в WHERE → индекс не используется.
   ===================================================================== */
