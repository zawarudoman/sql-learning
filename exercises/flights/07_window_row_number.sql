/* =====================================================================
   Задание 7. Нумерация рейсов внутри маршрута     ⭐ ОКОННЫЕ ФУНКЦИИ
   Тема: row_number() OVER (PARTITION BY ... ORDER BY ...)
   ---------------------------------------------------------------------
   ЗАДАЧА:
   Для каждого маршрута (route_no) пронумеруй его рейсы по времени
   планового вылета — от самого раннего (номер 1) к поздним.
   Столбцы: route_no, flight_id, scheduled_departure, номер.
   Ограничь вывод 50 строками, отсортируй по route_no и номеру.

   Вспомни правило:
     PARTITION BY = "для каждого ЧЕГО считаем отдельно"
     ORDER BY     = "в каком ПОРЯДКЕ нумеровать"
   ===================================================================== */

-- твоё решение:

